# ~/.claude/runbooks/

Reference material — **NOT auto-loaded** into CLAUDE.md context.

Files here are consult-on-demand: open them when the situation calls for it (debugging a known CC bug, reviewing Challenger output for a borderline case, retrofitting an old deploy script). They contain history, post-mortems, eval data, and long reference templates that are too heavy for the always-on system prompt.

For enforceable rules that ARE auto-loaded, see `~/.claude/rules/`.
